Simple processor which has some registers, adder and a control unit (and a clock cyle). 

Control unit receives instructions as input and executes them in the circuit.

This processor has 4 registers R0, R1, R2, R3. 

4 instructions (Load, Move, Add, Sub). 